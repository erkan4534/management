server:
  port: 8085
spring:
  datasource:
    username: root
    password: root
    url: jdbc:postgresql://127.0.0.1:5432/schoolDb
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
